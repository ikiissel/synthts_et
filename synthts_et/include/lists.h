#include <iostream>
#include <vector>
#include <string>
//using std::vector;
//void phrases (const std::string& input);


void search_abbrev (std::string& s, std::vector<std::string>& vec, bool with_explode );
void search_singles (std::string& s, std::vector<std::string>& vec );
